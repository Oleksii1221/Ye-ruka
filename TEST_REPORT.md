# Test report

Дата перевірки: 15 червня 2026 року

## Перевірено

- 31 автоматизований тест пройдено;
- Python-модулі успішно скомпільовано через `compileall`;
- повне PySide6-вікно запускається в зібраному EXE;
- обидва образи ESP32 сформовано й включено до дистрибутива;
- вбудований `espflash 4.4.0` запускається із frozen-збірки;
- Windows-інсталятор встановлює застосунок, передає початкову мову й тему, створює ярлик та коректно видаляється;
- portable ZIP і SHA-256 сформовано;
- сервісна сторінка покрита тестами структури, діапазонів і станів керування.

## Апаратні обмеження

Фізичне прошивання та рух сервоприводів не входять до автоматичних тестів. Їх потрібно виконувати на цільовій ESP32 із механікою без навантаження, окремим живленням сервоприводів і доступною аварійною зупинкою.

## Команда перевірки

```powershell
python tools/project.py check
```
